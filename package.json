{
  "name": "@micro-js/gen-to-promise",
  "description": "Convert generator to promise.",
  "repository": "git://github.com/micro-js/gen-to-promise.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@micro-js/is-function": "^1.0.4",
    "@micro-js/is-generator-object": "^1.1.0",
    "@micro-js/is-promise": "^1.0.0",
    "@micro-js/slice": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
